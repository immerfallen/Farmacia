@model List<Pedido>

@{
    ViewData["Title"] = "Lista de pedidos";
}

<h2>Lista de pedidos</h2>

<table>
    <thead>
        <th>Id</th>
        <th>Data</th>
        <th>Clínica</th>
        <th></th>
    </thead>
    <tbody>
        @foreach (Pedido item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.DataPedido</td>
                <td>@item.Clinica.Id</td>
                <td><a href="/Pedido/Detalhe?id=@item.Id">Detalhes</a></td>
            </tr>
        }        
    </tbody>
</table>